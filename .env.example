# Copia este fichero a .env y rellena los valores
# Genera secretos con: openssl rand -base64 32

PORT=3000

# MongoDB
DB_CONNECTION=mongodb://localhost/CW

# Secreto de sesión (larga y aleatoria)
MY_SECRET=pon_aqui_un_secreto_largo_y_aleatorio

# Usuario(s) master separados por comas
MASTERS=nombre_usuario_master

# API key para la API externa de carga de puzzles
EXTERNAL_API_KEY=pon_aqui_una_api_key_segura
